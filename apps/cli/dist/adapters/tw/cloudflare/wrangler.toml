# Cloudflare Pages deployment for TW Framework
# Generated by `tw adapter cloudflare`.
#
# Build configuration (also settable in the Cloudflare dashboard):
#   Build command:      tw build
#   Build output dir:   .tw
#
# Deploy with git integration (push to deploy) or:
#   npx wrangler pages deploy .tw

name = "tw-app"
compatibility_date = "2024-11-01"
pages_build_output_dir = ".tw"

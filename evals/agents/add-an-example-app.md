# Agent Eval: add-an-example-app

**Task**: Add `examples/weather/` -- a page that renders a city name from a
`[city]` dynamic segment and an API route that returns JSON.

**Pass criteria (rubric, all required)**:
1. `examples/weather/` contains package.json (scripts: dev/build/start),
   README.md, home/[city]/page.tw, home/api/now/route.twm.
2. `bun ../../apps/cli/tw/bin.ts build` exits 0 from the example dir.
3. examples/README.md table has a new row.
4. No other file in the repo changed.
